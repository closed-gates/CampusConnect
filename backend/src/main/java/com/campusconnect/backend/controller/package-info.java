/**
 * REST Controllers layer.
 *
 * Responsibilities:
 * - Handle incoming HTTP requests.
 * - Delegate business logic to the Service layer.
 * - Return appropriate HTTP responses.
 * - Keep controllers lightweight (no business logic here).
 *
 * Naming convention: [Entity]Controller.java  (e.g., UserController.java)
 * Base URL pattern:  /api/[resource]          (e.g., /api/users)
 */
package com.campusconnect.backend.controller;
