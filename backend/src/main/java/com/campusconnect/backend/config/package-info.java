/**
 * Config layer — Application Configuration.
 *
 * Responsibilities:
 * - Define Spring @Configuration beans.
 * - Configure CORS, security, beans, and other application-level settings.
 *
 * Key config classes (Phase 1 stubs, expanded in Phase 2):
 * - CorsConfig.java        — CORS settings for React frontend
 * - SecurityConfig.java    — Spring Security (Phase 2)
 * - ApplicationConfig.java — Shared beans (e.g., PasswordEncoder, AuthManager)
 */
package com.campusconnect.backend.config;
